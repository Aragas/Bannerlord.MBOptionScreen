using System;

namespace MCM.Utils
{
    public static class ReflectionUtils
    {
        public static bool ImplementsOrImplementsEquivalent(Type type, Type baseType, bool includeBase = true) => false;

        public static bool ImplementsOrImplementsEquivalent(Type type, string fullBaseTypeName, bool includeBase = true) => false;

        public static bool Implements(Type type, Type baseType) => false;
    }
}