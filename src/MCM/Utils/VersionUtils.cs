using MCM.Abstractions;

using System;
using System.Collections.Generic;

using TaleWorlds.Library;

namespace MCM.Utils
{
    public static class VersionUtils
    {
        public static (Type Type, IVersion Version)? GetLastImplementation(ApplicationVersion version, IEnumerable<Type> types) => null;
    }
}